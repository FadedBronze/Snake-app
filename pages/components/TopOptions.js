import styles from '../../styles/TopOptions.module.css'

const TopOptions = () => {
    return ( 
        <header className={styles.TopOptionsMenu}>

        </header>
    );
}
 
export default TopOptions;